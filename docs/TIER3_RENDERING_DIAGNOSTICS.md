# Tier 3 Rendering Diagnostics

The visual now normalizes rendering failures into data, rendering, state, fetch, or unknown categories and reports a non-empty reason through Power BI's renderingFailed event.

The helper is dependency-free and does not modify the mobile exclusion zone.
